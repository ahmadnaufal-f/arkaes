{
  "name": "@arkaes/design-engineer-brief",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Build for the Design Engineer PDF. Deliberately outside the pnpm workspace (apps/*, packages/*) — nothing in the monorepo depends on it.",
  "scripts": {
    "build": "node build.mjs",
    "proof": "node build.mjs --proof"
  },
  "dependencies": {
    "playwright": "^1.50.0",
    "qrcode": "^1.5.4",
    "sharp": "^0.34.0"
  }
}
